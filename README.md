# OpenFlightSimulator
A flight simulator that uses real physics.


## A few notes:
- If OpenGL lags: be sure to check the vertices and indices count. After assigning vertexCount and indexCount, it worked fine.
